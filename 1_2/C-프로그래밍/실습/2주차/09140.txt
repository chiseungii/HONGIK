int 정수 char 글자 한 자 float 실수 보통 크기 double 실수 2배 크기
한글 한 자 영문 글자 두 자에 해당
조심.. double 읽을 때 %lf 필수

if (조건문) q;
	// 조건문이 참이면 q를 실행하라.
if (조건문) { p; q; }
	// 조건문이 참이면 p, q를 실행하라.
== 동일한지
|| 혹은

if (ch == 'C' || ch == 'C')
만약 ch에 대문자 C가 들어있거나 혹은 ch에 소문자 c가 들어있으면
둘 중 하나라도 참말이면 전체가 참말임.
a = b + c + d;	// + 더하기 - 빼기 * 곱하기 / 나누기

int i; char ch; float f; double d;
scanf("%c %f %f %f", &ch, &f, &f, &f);	// 수정해서 국영수 등 처리
